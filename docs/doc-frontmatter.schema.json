{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://hawkeyexl.github.io/moose-tracevals/doc-frontmatter.schema.json",
  "title": "moose-tracevals docs page frontmatter",
  "description": "Requires every published docs page to declare a non-empty title and description. Starlight-specific keys (sidebar, template, hero, and so on) are allowed but unconstrained.",
  "type": "object",
  "required": ["title", "description"],
  "properties": {
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 }
  },
  "additionalProperties": true
}
