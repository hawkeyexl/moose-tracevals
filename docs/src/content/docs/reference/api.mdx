---
title: API reference
description: Everything @hawkeyexl/agentevals exports, grouped by what it is for, with the intended entry points marked.
sidebar:
  order: 8
---

import { Aside } from '@astrojs/starlight/components';

**Scope:** the export surface. How to *use* it is [Build on agentevals](/agentevals/extend/).
Source of truth: `src/index.ts`.

```ts
import { runEvals, parseTraceFile, registerGrader } from "@hawkeyexl/agentevals";
```

ESM-only, Node.js 24+, types shipped. **★ marks an intended entry point** — the rest is exported
because the CLI is built from it and it is useful for tests, but those are the ones to reach for
first.

## Subpath exports

| Specifier | What it is |
|---|---|
| `@hawkeyexl/agentevals` | The API below. |
| `@hawkeyexl/agentevals/schemas/artifact-evals-0.2.json` | The current criteria-block schema. |
| `@hawkeyexl/agentevals/schemas/artifact-evals-0.1.json` | The previous version, for pinned consumers. |

## Engine

| Export | Notes |
|---|---|
| ★ `runEvals(options)` | Runs the whole pipeline, returns `RunReport`. Needs a `judge` unless `deterministicOnly`. |
| `EngineOptions` | `tracePath`, `projectDir?`, `env?`, `config`, `judge?`, `deterministicOnly?`. |

## Commands

The CLI's three commands, callable directly. Each pairs a runner with a renderer.

| Export | Notes |
|---|---|
| `runRun`, `RunCommandOptions`, `RunCommandResult` | `run`. `RunCommandOptions.judge` injects a judge. |
| `runFill`, `renderFill`, `FillOptions`, `FillReport`, `FillArtifactResult`, `FillRun`, `FillStatus`, `SharpeningNote` | `fill`. `FillOptions.providerInstance` injects a provider. |
| `runList`, `renderList`, `ListOptions`, `ListRun` | `list`. |

## Traces

| Export | Notes |
|---|---|
| ★ `parseTraceFile(path)` | Detect the format and parse, returning `Trace`. |
| `parseTraceContent(text)` | The same from a string. |
| `detectFormat(path)`, `detectContentFormat(text)` | Format sniffing on its own. |
| ★ `discoverTraces(options)` | Scan the session store. Returns `TraceListing[]`, newest first. |
| `homeDir(env?)` | Resolves `AGENTEVALS_HOME`, falling back to the OS home. |
| `slugFor(cwd)` | The project-slug rule: every non-alphanumeric character to `-`. |
| `pickTrace(options, promptFn?)` | The interactive picker. `promptFn` makes it testable. |
| `DiscoverOptions`, `TraceListing`, `PickerChoice`, `PromptFn` | |
| *(all of `trace/types`)* | `Trace`, `TraceSource`, `TraceFormat`, `FileAccess`, `TraceUsage`, and the event types. |

## Artifacts

| Export | Notes |
|---|---|
| ★ `resolveArtifacts(trace, options)` | Trace-driven resolution. Returns artifacts, coverage, warnings. |
| `discoverArtifacts(options)` | Static, project-only scan. What `fill` uses. |
| `PROJECT_RULES_FILENAMES` | `["CLAUDE.md", "AGENTS.md", "GEMINI.md"]`. |
| `ResolveOptions`, `DiscoverArtifactsOptions`, `DiscoveredArtifact`, `DiscoveryResult` | |
| `findGitRoot`, `findInTree`, `listInTree`, `safeRead`, `PRUNED_DIRS` | Filesystem helpers the resolvers share. |
| *(all of `artifacts/types`)* | `ResolvedArtifact`, `CoverageEntry`, `ArtifactType`, `ResolvedArtifacts`. |

## Criteria

| Export | Notes |
|---|---|
| `extractCriteria(artifact)` | Read and validate a `metadata.evals` block. |
| `appendArtifactCriteria(...)` | Append criteria to frontmatter. The one write path. |
| `ARTIFACT_EVALS_SCHEMA_ID`, `artifactEvalsSchemaPath` | The published schema's `$id` and on-disk path. |
| `Criterion`, `CriterionType`, `ExtractedCriteria`, `Severity`, `NewCriterion` | |

## Planning and graders

| Export | Notes |
|---|---|
| `planEvals(artifacts)` | Turn artifacts into `EvalPlan[]`. |
| `IMPLICIT_EVAL_NAME` | `"adheres-to-artifact"`. |
| ★ `registerGrader(grader)` | Add a kind at runtime. See [Custom graders](/agentevals/extend/custom-graders/). |
| `graderFor(kind)`, `listGraderKinds()` | |
| `EvalPlan` | |
| *(all of `graders/types`)* | `TraceGrader`, `GradeResult`, `Finding`, `TraceGraderContext`. |

## Judge

| Export | Notes |
|---|---|
| `makeTraceJudge(options)` | Build the ensemble judge. |
| `makeJudgeProvider(...)`, `providerSpecFor(...)` | Provider construction. `providerSpecFor` resolves identity without constructing, so a fully cached run needs no API key. |
| `renderTrace(trace, options?)` | The transcript as the judge sees it. |
| `JUDGE_SYSTEM_PROMPT`, `buildUserContent(plan, rendered)` | The prompts. |
| `PROMPT_VERSION` | Cache-key component. Bump on any prompt change. |
| `cacheKey(...)`, `sha256(...)` | |
| `TraceJudge`, `JudgedEval`, `TraceJudgeOptions`, `RenderOptions`, `MockResponse` | |

<Aside type="caution" title="`TraceJudge` is the seam, not `makeTraceJudge`">
Anything matching the `TraceJudge` function type works, which is what makes downstream suites
offline-able. You rarely need `makeTraceJudge` unless you want the real ensemble.
</Aside>

## Configuration

| Export | Notes |
|---|---|
| ★ `loadConfig(dir?)` | Read, validate, and fully populate defaults. |
| `parseConfig(raw)` | The same from an object. |
| `DEFAULT_CONFIG_FILENAME` | `"agentevals.config.yaml"`. |
| `AgentevalsConfig`, `ProviderConfig`, `Pricing` | |

Downstream code always receives a fully-populated config, so there is no need to re-apply defaults.

## Reporting and history

| Export | Notes |
|---|---|
| ★ `render(report, format)` | `"human"`, `"json"`, or `"markdown"`. |
| `ReportFormat` | |
| `appendHistory(file, report)`, `loadHistory(file)`, `compareToLast(...)`, `entryFor(report)` | |
| `HistoryEntry`, `HistoryEval`, `HistoryComparison` | |
| *(all of `types`)* | `AgentevalsError`, `RunReport`, `EvalResult`, `RunSummary`, `Outcome`. |

`AgentevalsError` is the operational-failure type. The CLI maps it to exit `2`; catch it if you want
to distinguish "the tool could not run" from "a check failed".

## `fill` internals

Exported so the authoring path is testable and extensible. Most consumers will not need these.

| Export | Notes |
|---|---|
| `gateProposals(...)`, `GateOptions`, `GateResult` | The gate, in order. |
| `ALLOWED_GRADERS` | Per-artifact-type grader allowlist. |
| `ProposedCriterion`, `Rejection`, `RejectionReason`, `Vocabulary` | |
| `PROPOSAL_SCHEMA`, `isValidProposal(...)` | The shape a proposal must match. |
| `systemPromptFor(type)`, `buildFillUser(...)` | |
| `FILL_PROMPT_VERSION`, `MAX_BODY_CHARS` | Cache-key components. |
| `FillCache`, `fillCacheKey`, `FillCacheKeyParts` | Separate from the judge cache. |
| `artifactFacts(...)`, `ArtifactFacts` | Grounding inputs, not criteria. |
| `buildVocabulary(...)`, `BUILTIN_TOOLS` | |
| `mockFillProposal` | The canned proposal `--provider mock` returns. |

<Aside title="Stability">
The package is pre-1.0 and versioned by semantic-release from Conventional Commits. The ★ entry
points are the ones the CLI itself is built on and the least likely to move; the `fill` internals
exist mostly to serve this repository's own tests.
</Aside>
