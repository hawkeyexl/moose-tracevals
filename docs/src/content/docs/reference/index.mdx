---
title: Reference
description: Complete lookup reference for the moose-tracevals CLI, configuration file, deterministic graders, criteria schema, and report formats.
sidebar:
  order: 1
---

import { LinkCard, CardGrid } from '@astrojs/starlight/components';

**Scope:** exhaustive lookup. These pages answer *what are the options?* They do not explain when
to reach for one. For that, start from a journey: [Get started](/moose-tracevals/get-started/),
[Declare what to check](/moose-tracevals/declare/), [Add moose-tracevals to CI](/moose-tracevals/ci/), or
[How the judge decides](/moose-tracevals/judge/).

<CardGrid>
  <LinkCard
    title="CLI"
    description="run, fill, and list: every argument, flag, and default."
    href="/moose-tracevals/reference/cli/"
  />
  <LinkCard
    title="Configuration"
    description="Every moose.config.yaml key under tracevals, including the ones with no CLI flag."
    href="/moose-tracevals/reference/configuration/"
  />
  <LinkCard
    title="Graders"
    description="All seven deterministic kinds: options, validation rules, and exact failure messages."
    href="/moose-tracevals/reference/graders/"
  />
  <LinkCard
    title="Criteria schema"
    description="The metadata.evals frontmatter block, field by field, across both published versions."
    href="/moose-tracevals/reference/criteria-schema/"
  />
  <LinkCard
    title="Reports & exit codes"
    description="The human, JSON, and markdown report shapes, the exit-code contract, and the history file."
    href="/moose-tracevals/reference/report-and-exit-codes/"
  />
</CardGrid>

## Reading these pages

- **Defaults are the resolved values.** Every knob flows through the config file, which fills in
  every default before anything reads it. A CLI flag **overrides** the resolved config instead of
  bypassing it, so the default shown for a flag is the config default it would replace.
- **Unknown configuration keys are errors.** The config schema rejects additional properties at
  every level, so a typo fails loudly instead of being ignored.
- **Deterministic grader options are validated before grading.** A malformed criterion produces an
  `error` outcome naming the offending option, never a silent pass.
